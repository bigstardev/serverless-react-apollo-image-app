{
  "name": "serverless-apollo-gql-app-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "serverless offline start --config serverless.local.yml",
    "build:prod": "echo \"Not needed to be build\" && exit 0",
    "deploy": "serverless deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server": "^2.15.1",
    "apollo-server-lambda": "^2.15.1",
    "aws-lambda": "^1.0.6",
    "dotenv": "^8.2.0",
    "graphql": "^15.3.0"
  },
  "devDependencies": {
    "serverless-offline": "^6.4.0"
  }
}
